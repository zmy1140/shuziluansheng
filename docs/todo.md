# 后续任务清单

最后更新：2026-06-10

## 执行原则

- 先做 P0，再做 P1，最后才考虑 P2。
- 每次只拿一个明确任务做，不要一次性重构整个项目。
- 任何代码修改完成后，至少运行 `npm.cmd test` 和 `npm.cmd run build`。
- 每次任务完成后立即更新 `docs/context.md`；如果任务状态变化，同步更新本文件。
- 后续科研、设计、代码和数据处理任务完成后，同步维护 `docs/design_log.md`，必要时新增 `docs/design_reports/` 下的阶段性设计报告。
- 当前优先级不是继续美化界面，而是让项目进入可长期维护、可接入实验数据的状态。

## P0：必须马上做

- 按 2026-06-01 师兄视频要求，近期一周多到半个月的主线先转向真实传感器接入：至少把六维力、三轴加速度、声发射等传感器的数据读出来并接到当前软件/网页显示链路中，先让基本功能可用。
- 核对每个传感器/采集设备输出到底是工程量、数字量、还是电压/电流原始量；如果是电压/电流，需要明确标定关系、配套采集仪/软件和对比校准方案。
- 确认源码中文编码问题：用 VS Code 和浏览器实际显示核对 `index.html`、`src/app.js`、`src/main.js`、`src/app.test.js` 中的中文文案；如果只是 PowerShell 显示乱码，不要修改；如果源文件确实损坏，再小范围修复。
- 和师兄/导师确认当前“本地工控监测软件 + 3D 工位视窗”的界面方向是否符合汇报和课题预期。
- 确认采集与控制硬件链路：核对“汇川 AI820”的准确型号、输入类型、采样频率、通信协议，以及三轴振动和声发射是否需要独立高速采集卡。
- 明确第一版粗糙度预测演示目标：只展示预测结果，还是需要能读取一段实验数据并输出 Ra。
- 在传感器能读数后，设计并执行第一批实际打磨测试，保存力、振动、声发射、加工参数和粗糙度实测标签，为后续仿真校准和粗糙度预测提供真实数据。
- 结合师兄视频要求，继续明确后续真实仿真边界：当前已完成第一版 Abaqus 简化移动热源温度场链路，后续需要确认真实热源功率、材料热参数、接触/摩擦热比例、散热边界和验证方式；该项可与传感器接入并行，但不应压过真实数据采集主线。

## 已完成

- 2026-05-15：初始化 Git 仓库，新增 `.gitignore`，排除 `node_modules/`、`dist/`、`.vite/`、`coverage/`、日志、编辑器配置和本地 Codex/plugin 状态目录。
- 2026-05-15：创建首次提交并推送到 GitHub 仓库 `https://github.com/zmy1140/shuziluansheng.git`，本地 `main` 已跟踪 `origin/main`。
- 2026-05-15：补充最小 `README.md`，说明项目定位、启动测试命令、GLB 导入、离线边界、主要目录和后续方向。
- 2026-05-15：将工况总览“实时趋势”从单条示意曲线改为力、振动、声发射、Ra 预测四条归一化模拟曲线，并增加图例和测试。
- 2026-05-15：修复实时趋势图例颜色被通用标题标签样式覆盖的问题，并将 UDP 通信区块明确标注为后续接口预留、当前未接入真实设备。
- 2026-05-15：在不降低 3D 视图高度的前提下压缩工况总览布局，将趋势和事件移入 3D 右侧信息列，减少纵向滚动。
- 2026-05-15：将“报警与事件”移入最右侧状态栏，并把右侧 UDP 通信、报警阈值、运行状态、报警与事件改为默认收起的折叠面板。
- 2026-05-15：重构各传感器详情页为技术诊断视图：力页强调接触稳定性，振动页加入 FFT 频谱，声发射页强调高频能量和突发事件，主轴页强调转速偏差/电流负载，粗糙度页强调输入窗口和特征摘要。
- 2026-05-18：初步核对 YE6275D 采集器手册，判断其适合三轴加速度振动原始波形采集；普通 PLC 模拟量模块更适合低频状态/控制量，不建议替代高速振动采集。仍需向供应商或设备资料确认“汇川 AI820”的准确型号和规格。
- 2026-05-18：确认本机 Abaqus 2025 命令行可用，并建立 `abaqus_runs/minimal_cantilever/` 最小 Abaqus/Standard 测试模型；求解器提交成功，ODB 后处理脚本可导出 JSON/CSV 结果，为后续把仿真结果接入前端打通了最小链路。
- 2026-05-18：新增 `abaqus_runs/visible_smoke_test/` 可见进度 smoke test，通过独立可见命令窗口运行 Abaqus/Standard，并打开 Abaqus/CAE/ODB；确认用户可以看到求解进度，后处理仍可导出 JSON/CSV。
- 2026-05-18：建立持续设计报告体系，新增 `docs/design_reports/2026-05-18_阶段性设计报告.md`、`docs/design_log.md` 和 `docs/下一步任务清单.md`，用于后续向导师汇报或整理开题、中期、结题材料。
- 2026-05-18：完成第二阶段 3D 加工过程动画原型：厚板工件、圆柱磨头、螺旋向内收缩轨迹、运动痕迹、模拟粗糙度风险色块、速度控制和重置；导入 GLB 后按包围盒拟合加工平面。
- 2026-05-18：完成 Abaqus 简化厚板仿真结果颜色映射原型：新增 `abaqus_runs/plate_color_mapping/` 和 `public/simulation/plate_color_mapping.json`，前端可读取简化厚板 Mises 应力样本并映射到颜色色块；该结果仅用于链路验证，不代表真实磨抛仿真或真实粗糙度预测。
- 2026-05-19：基于第二阶段动画和 Abaqus 映射原型，更新阶段性设计报告、设计日志和下一步任务清单；补充设计决策、关键参数、映射公式、问题排查和待确认事项。本次只修改文档，未修改业务代码。
- 2026-05-19：新增 `docs/实验数据字段表.md`，基于 AI820、YE6275D、CA-YD-3EC3001、M8229、RBZ-E30-B80 等现有手册建立第一版采集归档字段表草案；当前仍无真实采集数据，声发射资料暂缺，相关字段均标注待确认。
- 2026-05-19：用户确认第一版颜色渲染任务的目标以“温度场渲染”为例；后续汇报和前端说明应将颜色含义从泛泛的“风险/应力/云图”收敛为温度场示例，同时继续标注为演示或待真实仿真验证。
- 2026-05-24：完成第一版局部打磨温度场演示：固定工具模型 `public/models/tool.glb` 自动加载，路径由 `data/demo/line_grinding_path.csv` 经 `npm.cmd run convert:path` 转为 `public/paths/line_grinding_path.json`，前端读取 `public/simulation/temperature_demo.json` 将演示温度场映射到平板/展开面网格颜色；当前仍为演示数据，不代表真实 Abaqus 或实验温度。
- 2026-05-25：确定温度场/仿真结果 CSV 到前端 JSON 的第一版格式。新增 `src/temperature.js`、`scripts/convert-temperature-csv.js`、`data/demo/temperature_field.csv`、`public/simulation/temperature_field.json` 和 `docs/temperature-field-format.md`；标准 CSV 表头为 `x_mm,z_mm,temperature_c`，可由 Abaqus 后处理或实验测温整理脚本导出，再通过 `npm.cmd run convert:temperature` 转为前端 JSON。
- 2026-05-26：复核温度场 CSV/JSON 链路中文编码，确认 `src/temperature.js`、温度生成脚本和温度 JSON 实际为正常 UTF-8 中文；普通 PowerShell `Get-Content` 可能显示乱码。已更新 `src/temperature.test.js`，用正常中文断言保护 `valueLabel` 和 `note` 字段。
- 2026-05-26：完成第一版 Abaqus 简化移动热源温度场链路。新增 `abaqus_runs/moving_heat_source_plate/`，生成 `100 mm x 100 mm x 8 mm` 平板、`40 x 40 x 2` DC3D8 热传导单元、`24 mm` 等效圆形移动接触温度边界，并通过 ODB 后处理导出 `x_mm,z_mm,temperature_c` CSV；`npm.cmd run convert:temperature` 已生成前端 JSON。前端优先读取 Abaqus 简化温度场，失败时回退演示温度场。该结果使用占位材料和热源参数，不代表真实实验温度。
- 2026-06-03：从采购资料目录 `D:\Desktop\进气道项目\08传感器等采购` 筛选并归档传感器/采集/主轴/材料相关技术资料到 `docs/equipment_reference/`，并新增索引 `docs/equipment_reference/README.md`。已归档 M3553C、M8229、iDAS、CA-YD-3EC3001、YE6275D、RBZ-E30-B80、SAC2-NP1 和涂层性质表等资料；YE6231G 手册仅作为历史/对照资料保留，不作为当前实验采集设备；安装包、报价单、合同和无关大文件未纳入。
- 2026-06-08：YE6275D 与电脑以太网直连采集链路已跑通，电脑有线网卡设置为 `192.168.99.100/24` 后可 ping 通采集器 `192.168.99.121`；YE7602 已能启动采集并导出第一份三轴加速度 txt 样例 `0608data01.txt`。该样例已复制到 `D:\Desktop\进气道项目\实验数据\2026-06-08_YE6275D三轴加速度\`，并生成带表头 CSV。后续仍需补采空载、轻敲、三轴方向激励和实际加工状态样例。
- 2026-06-08：完成三轴加速度真实样例到前端的第一版窗口回放链路。`npm.cmd run convert:acceleration` 会把 `data/sanzhoujiasudu_data` 下三组 `_xyz.csv` 转换为 `public/simulation/acceleration_features.json`；网页振动卡片和“振动分析”页现在读取 YE6275D 真实导出样例的 1 秒窗口特征并循环回放。当前仍只代表采集链路验证，不代表正式打磨实验数据。
- 2026-06-10：振动页支持从浏览器选择本地 YE6275D 三列 txt/csv 并直接转换为窗口特征回放；新增 `npm.cmd run convert:acceleration:file -- "D:\path\to\data.txt"`，可把任意 txt/csv 转为同目录同名 JSON，已有 JSON 时复用。
- 2026-06-10：振动页回放控件改为导入优先的空状态，页面启动时不再默认选择“手动晃动传感器（15 s）”；网页端不再暴露内置样例按钮。
- 2026-06-10：删除振动页“下载JSON”按钮；网页端只负责导入、缓存和回放，同目录 JSON 文件继续通过 `npm.cmd run convert:acceleration:file` 生成。
- 2026-06-10：删除振动页“加载内置样例”按钮；页面操作区只保留“导入TXT/CSV”。
- 2026-06-10：将振动页“导入TXT/CSV”按钮移动到“回放数据”下拉框同一行，放在“请先导入TXT/CSV”旁边。
- 2026-06-10：将“切削力 > 合力时域波形”改为参考 ZLG/CAN 通信软件的多通道曲线窗口，包含 CH1-CH6 彩色曲线、显示设置表和放大/缩小/复位操作。
- 2026-06-10：压缩切削力页左侧“合力”读数卡片，扩大合力时域波形的显示空间，使波形图占页面主要区域。
- 2026-06-10：将切削力页“合力”信息从左侧栏改为顶部横向摘要条，放在合力时域波形上方。
- 2026-06-10：完成六维力 iDAS 导出文件到前端回放的第一版链路。新增 `npm.cmd run convert:force:file -- "D:\path\to\idas.txt" --sample-rate 200`，可将 M8229/iDAS 的 `CH1-CH6` 六维力 txt/csv 转为同目录 JSON；“切削力”页新增 iDAS 六维力 TXT 导入控件，按 200 Hz 和 1 秒窗口回放 Fx/Fy/Fz、合力均值和峰峰值。本轮已将 `D:\iDAS R&D\Data\10sqingya.txt` 转为 `D:\iDAS R&D\Data\10sqingya.json`。
- 2026-06-10：简化“切削力 > iDAS 六维力数据回放”面板，删除与顶部摘要条重复的回放窗口、Fx、Fy、Fz、合力均值和峰峰值信息卡；六维力关键指标统一在顶部横向摘要条显示。
- 2026-06-10：更新六维力链路资料归档：M8229 采集卡手册由 V1.0 更新为 V2.1，iDAS R&D 手册由 20260225 更新为 20260525，并新增 `M8229f32_ethercat_esi.xml`；M3553C 手册和六维力选型表经哈希核对与源目录一致，未重复替换。

## P1：近期做

- 持续维护设计报告：每次完成重要科研、设计、仿真、实验、代码或数据处理任务后，更新 `docs/design_log.md`，必要时新增阶段性设计报告。
- 继续复核第一版局部打磨温度场演示效果，重点检查固定工具 GLB、直线路径、速度控制、重置、温度色块和 GLB 导入后包围盒拟合是否符合汇报预期。
- 基于第一版 Abaqus 简化移动热源链路，继续把占位接触温度边界升级为真实热流功率、摩擦生热比例或热-力接触模型，并补充材料热参数和散热边界依据。
- 使用真实采集数据或设备导出样例验证 `docs/实验数据字段表.md`，重点核对字段名、单位、采样率、通道映射和时间同步方式。
- 基于第一版数据字段继续完善详情页：振动页已接入 YE6275D 三轴加速度窗口特征；后续继续把力、声发射、主轴和 Ra 输入特征从模拟占位替换为真实数据或设备导出样例。
- 以 iDAS 六维力样例为对照，在 AI820/InoProShop 中配置 M8229 EtherCAT 从站，读取 `DataNo/Fx/Fy/Fz/Mx/My/Mz`，并与 iDAS 同动作样例比较方向和量级。
- 增加导航切换、模型导入、离线资源检查等测试用例。
- 设计前端与后端/预测模型之间的数据接口草案，但暂不急于实现后端。
- 整理一份给导师或师兄看的“第一阶段演示说明”：当前能展示什么、哪些是模拟数据、下一步如何接真实数据。
- 设计第一版实验数据目录结构，例如 `data/raw/`、`data/demo/`、`data/labels/`，但创建前需确认是否纳入当前仓库。
- 明确振动 FFT、声发射 Hits/Counts 和高频能量等特征由前端计算、算法服务计算，还是由采集设备直接输出。
- 补齐 `M3553C` 六维力传感器标定参数、M8229 EtherCAT 对象字典/过程数据映射或串口寄存器说明、声发射设备资料、粗糙度 Ra 测量方案和主轴 Modbus 寄存器地址。

## P2：以后再做

- 接入真实 UDP 或串口/采集卡数据流。
- 搭建 FastAPI 后端和 SQLite 数据存储。
- 训练或接入粗糙度预测基线模型，例如传统机器学习或轻量深度学习模型。
- 将预测模型输出与前端 Ra 展示、异常提示、趋势曲线联动。
- 做实验数据管理：实验编号、工件编号、加工参数、传感器文件、粗糙度测量结果关联。
- 封装为更适合离线实验室使用的桌面应用或本地启动脚本。
- 后期制作“任意电脑可直接打开”的离线交付包，优先评估 Vite 相对路径静态包、便携本地服务启动脚本或桌面应用封装三种方案。
- 优化 3D 场景：真实工位模型、工具路径、接触点、传感器位置、状态颜色映射。
- 后期实现工件与蛇形臂运动可视化：导入真实工件和蛇形臂分段模型，定义关节/段姿态数据格式，将 PLC 或本地算法输出的运动状态映射到 Three.js 模型动画。
- 后续如需要论文或开题材料，再把界面和数据流程整理成技术路线图。
